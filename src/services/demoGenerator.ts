/**
 * ডেমো ডেটাসেট সার্ভিস ও প্রবেশদ্বার (Demo Dataset Service)
 * Exports the complete deterministic dataset generator and validations.
 */

export * from '../data/demoGenerator';
export {
  DEFAULT_DEMO_DATA_SEED,
  PILOT_DISTRICTS,
  TOTAL_CASES_TARGET,
  TOTAL_LAWYERS_TARGET,
} from '../data/demoConfig';
